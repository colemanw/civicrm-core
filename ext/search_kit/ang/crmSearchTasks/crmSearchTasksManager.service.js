(function(angular, $, _) {
  "use strict";

  // Trait shared by task controllers
  angular.module('crmSearchTasks').factory('crmSearchTasksManager', function(crmApi4) {
    var ts = CRM.ts('org.civicrm.search_kit');

    return {

    };

  });

})(angular, CRM.$, CRM._);
